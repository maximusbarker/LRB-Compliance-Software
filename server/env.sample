PORT=4000
DATABASE_URL=sqlite://./dev.db
JWT_SECRET=change-me
UPLOAD_DIR=../uploads
ALLOW_ORIGIN=http://localhost:3000

# Email (set EMAIL_MODE=smtp to enable sending)
EMAIL_MODE=manual
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=


